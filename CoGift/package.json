{
  "version": "0.0.1",
  "name": "CoGift",
  "private": true,
  "devDependencies": {
    "bower": "^1.4.1",
    "browserify": "^10.1.3",
    "catw": "^1.0.0",
    "less": "^2.5.0",
    "less-plugin-autoprefix": "^1.4.1",
    "less-plugin-glob": "^1.1.0",
    "parallelshell": "^1.1.1",
    "tsify": "^0.8.2"
  },
  "scripts": {
    "env": "env",
    "start": "dnx . kestrel",
    "build:scripts": "browserify scripts/common.ts -p [ tsify ] > wwwroot/js/common.js",
    "build:styles": "lessc styles/common.less wwwroot/css/common.css -x --autoprefix='last 2 versions' --glob",
    "build": "parallelshell 'npm run build:styles' 'npm run build:scripts'"
  }
}
